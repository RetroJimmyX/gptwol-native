# Native deployment work

This branch is for making GPTWOL run as a normal Linux application without requiring the Docker container layout.

The goal is to preserve the application's functionality while moving container-specific paths and assumptions into explicit configuration.
